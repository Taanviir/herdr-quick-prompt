{
  "name": "herdr-quick-prompt",
  "version": "0.2.0",
  "private": true,
  "type": "commonjs",
  "description": "Herdr plugin: pick a coding agent, type a prompt, launch it in a new tab",
  "license": "MIT",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
